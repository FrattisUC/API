'use strict';

module.exports = function(Course) {
  // Se puede poner cualquier nombre - No hay validación para name
  // Se puede poner cualquier número a la sección - No hay validación para section
  // Puede ser un curso de cualquier año - No hay validación para year
  // Periodo asumo que solo hay 1 y 2, pero podrían agregarse períodos 
  // como 0 para tav o 3 o 4 para tai si se hiciera en el futuro
  // Por eso no hay validación para period, para dar libertad
  
};

