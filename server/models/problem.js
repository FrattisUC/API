'use strict';

// Decidimos que para crear un test se le va a crear AL problema
// Los tests no se van a crear por si solos y luego agregar

module.exports = function(Problem) {
  // Se puede poner cualquier nombre al problema - No tiene validación name
  // El largo de la descripción del problema no está limitado - No hay
  // validación para description
  
};
