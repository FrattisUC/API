'use strict';

module.exports = function(Test) {
  // Es libre el input que recibe - No hay validación para input
  // Es libre el output que entrega - No hay validación para el output

  // Ya que decidimos que para crear un test se le va a crear AL problema,
  // los tests no se van a crear por si solos y luego agregar - No hay 
  // validación de problem_id

};
